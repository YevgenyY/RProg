}
pollutantmean("specdata", "aaa", 1:10)
getwd()
setwd("paste(directory,sprintf("%03.0f.csv",n),sep="/")")
setwd("..")
getwd()
pollutantmean("specdata", "aaa", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
mymean <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
data
}
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
}
pollutantmean("specdata", "aaa", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
mymean <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
mean(data[,2])
}
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
}
pollutantmean("specdata", "aaa", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
mymean <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
result <- mean(data[,2])
}
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result
}
pollutantmean("specdata", "aaa", 1:10)
data<-read.csv("specdata/001.csv")
mean(data[,2])
mean(data[,2], na.rm=TRUE)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
mymean <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
result <- mean(data[,2], na.rm=TRUE)
}
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result
}
pollutantmean("specdata", "aaa", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
mymean <- 0
colid <- if(pollutant == "sulfate")
1
else
2
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
result <- mean(data[,2], na.rm=TRUE)
}
result
}
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
mymean <- 0
colid <- if(pollutant == "sulfate")
1
else
2
colid
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
result <- mean(data[,2], na.rm=TRUE)
}
result
}
pollutantmean("specdata", "aaa", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
mymean <- 0
colid <- if(pollutant == "sulfate")
1
else
2
colid
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
result <- mean(data[,colid], na.rm=TRUE)
}
result
}
pollutantmean("specdata", "aaa", 1:10)
pollutantmean("specdata", "sulfate", 1:10)
first <-read.csv('001.csv')
first <-read.csv('specdata/001.csv')
first$sulfate
first$nitrate
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
mymean <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
result <- mean(data$pollutant, na.rm=TRUE)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
data
View(data)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
mymean <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
result <- mean(data$eval(pollutant), na.rm=TRUE)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
mymean <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
result <- mean(data[,eval(pollutant)], na.rm=TRUE)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- mean(result, tmp)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- if (result == 0 )
tmp
else
mean(result, tmp)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
tmp
result <- if (result == 0 )
tmp
else
mean(result, tmp)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- c(result, tmp)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
mean(pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- if (result == 0 )
tmp
else
mean(result, tmp)
}
result
})
mean(pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- if (result == 0 )
tmp
else
mean(result, tmp)
}
result
})
mean(pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- if (result == 0 )
tmp
else
mean(result, tmp)
}
result
})
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- c(result, tmp)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- if (result == 0)
tmp
else
c(result, tmp)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- 0
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- if (length(result) == 0)
tmp
else
c(result, tmp)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- list()
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- if (length(result) == 0)
tmp
else
c(result, tmp)
}
result
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
result <- list()
for (n in id) {
## print(paste(directory,sprintf("%03.0f.csv",n),sep="/"))
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
result <- if (length(result) == 0)
tmp
else
c(result, tmp)
}
mean(result)
}
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean("specdata", "nitrate", 70:72)
pollutantmean("specdata", "nitrate", 23)
data <- read.csv(paste(directory,sprintf("%03.0f.csv",n),sep="/"));
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
data <- read.csv("specdata/023.csv");
tmp <- mean(data[,eval(pollutant)], na.rm=TRUE)
data <- read.csv("specdata/023.csv");
data
data
View(data)
mean(data$nitrate, na.rm=TRUE)
data <- read.csv("specdata/070.csv")
mean(data$nitrate, na.rm=TRUE)
data <- read.csv("specdata/071.csv")
mean(data$nitrate, na.rm=TRUE)
data <- read.csv("specdata/072.csv")
mean(data$nitrate, na.rm=TRUE)
mean(c(1.280833, 0.2551667, 1.431601, 2.363205)
mean(c(1.280833, 0.2551667, 1.431601, 2.363205))
mean(c(1.280833, 0.2551667, 1.431601, 2.363205))
pollutantmean("specdata", "nitrate", 70:72)
data
a<-c(1,2,3)
b<-c(4,5,6)
x<-rbind(a,b)
x
